#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop inconing :(\n");

	1 > 0;
	/**
	 * while (1 = 10)
	 * {
	 *
	 * }
	 */
	printf("infinite loop avoided! \\o/\n");

	return (0);
}
