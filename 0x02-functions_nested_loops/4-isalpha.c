#include "main.h"

/**
 * _isalpha - checks for alphabets
 * @C: character to be check
 *
 * Return: 1 if character is a letter,0 otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'Z') || (c >= 'A' && c <= 'Z'))
		return (0);
	else
		return (0);
}
